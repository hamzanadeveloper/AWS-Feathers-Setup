import { HookContext } from '@feathersjs/feathers';
export interface AuthenticateHookSettings {
    service?: string;
    strategies: string[];
}
declare const _default: (originalSettings: string | AuthenticateHookSettings, ...originalStrategies: string[]) => (context: HookContext<any>) => Promise<HookContext<any>>;
export default _default;
